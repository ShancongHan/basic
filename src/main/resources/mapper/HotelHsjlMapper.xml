<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.basic.dao.HotelHsjlDao">

    <select id="selectAll" resultType="com.example.basic.entity.HotelHsjl">
        SELECT hotel_id, hotel_name, telephone, city_name, longitude as latitude, latitude as longitude, address
        FROM `hotel_hsjl`
    </select>
</mapper>